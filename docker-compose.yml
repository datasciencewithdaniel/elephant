version: '3.3'
services:
    elephant:
        container_name: elephant
        ports:
            - '3001:3000'
        volumes:
            - '${PWD}:/code'
        image: elephant
        stdin_open: true
        tty: true